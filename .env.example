# Copy to `.env` and fill in. Then either:
#   export $(grep -v '^#' .env | xargs)
#   or:  set -a && source .env && set +a
#
# Parallel CLI: https://docs.parallel.ai/integrations/cli

PARALLEL_API_KEY=
